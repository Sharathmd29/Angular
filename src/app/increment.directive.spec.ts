import { IncrementDirective } from './increment.directive';

describe('IncrementDirective', () => {
  it('should create an instance', () => {
    const directive = new IncrementDirective();
    expect(directive).toBeTruthy();
  });
});
